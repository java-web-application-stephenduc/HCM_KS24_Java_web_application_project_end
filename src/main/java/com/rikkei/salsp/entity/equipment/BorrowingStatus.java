package com.rikkei.salsp.entity.equipment;

/**
 * Enum định nghĩa trạng thái của phiếu mượn thiết bị (PENDING_DISPATCH, DISPATCHED, RETURNED, OVERDUE).
 */
public enum BorrowingStatus {
    PENDING_DISPATCH,
    DISPATCHED,
    RETURNED,
    OVERDUE
}

